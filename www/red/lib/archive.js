/*
function generateRelations() {

	data.relations.forEach(function(d) {
		
		if(d.source != 0) {
			d.source = _.findWhere(data.friends, {id: d.source});
		} else {
			d.source = meData;
		}
		
		if(d.target != 0) {
			d.target = _.findWhere(data.friends, {id: d.target});
		} else {
			d.target = meData;
		}

	});

};
*/