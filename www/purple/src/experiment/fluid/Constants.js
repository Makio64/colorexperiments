define(function() {
    return {
        GRAVITY: 0.05,
        RANGE: 16,
        // RANGE_SQ: 32,
        DENSITY: 2.5,
        PRESSURE: 1,
        PRESSURE_NEAR: 1,
        VISCOSITY: 0.1
        // NUM_GRID: 18,
        // INV_GRID_SIZE: 1 / (this.width / this.NUM_GRID),
    };
});